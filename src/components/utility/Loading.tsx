import Image from 'next/image';

export default function Loading() {
  return (
   <h1 className='mx-auto text-center justify-center h-100 w-500 animate-pulse text-2xl transition duration-300'>
   "Loading... as slow as a snail on a coffee break."</h1>
  );
}
